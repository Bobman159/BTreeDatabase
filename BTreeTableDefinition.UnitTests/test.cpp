#include "pch.h"
#include "bTreeTable.h"

TEST(CreateTableTests, CreateTableTest) {
//    struct TableDefinition* tableP = createTable("Meals");
    createTable("Meals");
    //EXPECT_EQ(tableP->tableName, "Meals2");
    //EXPECT_FALSE(tableP == NULL);
}